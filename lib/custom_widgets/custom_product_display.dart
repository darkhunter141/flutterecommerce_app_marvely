class custom_product_display {
  String title, imgurl;
  int proprice, rating;
     double discount;

  custom_product_display(
      this.title, this.imgurl, this.rating, this.proprice, this.discount);
}
